# NFC Guard — 스캐폴딩 요약

## 구조
- `android/` — 본체. HCE 필드 감지 + 보행성분 제거 기반 제스처 판별 + 비차단형 사후 리포팅.
- `ios/NfcGuardLite/` — 축소판. 자동 탐지 불가(플랫폼 한계)로 수동 점검/셀프 리포트/교육 콘텐츠로 스코프 조정.

## Android 핵심 흐름
1. `MotionBuffer` — 가속도계/자이로 상시 링버퍼 (2.5초 창)
2. `NfcGuardHceService.processCommandApdu()` — 필드 감지 시 버퍼 스냅샷 즉시 조회 (신규 측정 없음 → 지연 0에 수렴)
3. `GestureMatcher` — 보행 주기(1.3~2.6Hz) 대역 제거 → 잔차 이중적분으로 순변위 추정 + 자세변화량 → 위험도 스코어
4. `AnomalyReporter` — 트랜잭션을 절대 막지 않고 비동기 리포트만 전송, 반복 빈도 누적 시에만 사용자 알림
5. `OnboardingActivity` — 사용자별 태깅 제스처 baseline 학습 (오탐 감소)

## 지금 당장 반드시 확인/검증해야 할 것 (아직 안 끝난 부분)
- **AID 충돌**: 실제 결제 AID를 이 서비스가 직접 소유하면 안 됨. `apduservice.xml`은 데모 AID만 사용 중.
  실서비스로 가려면 "결제 트랜잭션과 병렬로 필드 감지만 관찰"하는 구조를 NFC 칩 벤더/단말 제조사와
  협의해야 함 — 일반 서드파티 앱 권한만으로는 안드로이드에서도 완전한 병렬 관찰이 보장되지 않는 기종이 있음.
- **밴드리젝트 필터 튜닝**: 지금 구현은 단순 이동평균 기반 근사. 실제 보행 데이터로
  FFT 기반 정밀 필터 + 개인별 보폭 주파수 캘리브레이션 필요.
- **드리프트 문제**: 이중적분 기반 변위 추정은 근본적으로 부정확함 (물리 법칙상 한계).
  현재는 "짧은 창"으로 완화했지만, 실사용 데이터로 임계값 재검증 필수.
- **오탐/미탐 실측**: 실제 지하철 환경 데이터로 ROC 커브 뽑아서 RISK_REPORT_THRESHOLD 값 재조정 필요.
- **백엔드 연동**: `AnomalyReporter.postToBackendStub()`는 자리만 잡아둔 상태. 실제 카드사/발급사와
  이상거래탐지(FDS) 연동 계약이 있어야 "세션 무효화"가 실질적 효과를 가짐 (개인 개발자가 임의로
  카드사 세션을 무효화할 권한은 없음 — 이 부분이 사업화 시 가장 큰 장벽).

## iOS가 자동 탐지를 할 수 없는 이유 (재확인)
- Apple Wallet/Secure Element 트랜잭션 이벤트는 서드파티 앱에 비공개
- Core NFC reader session은 사용자 명시적 시작 필요, 백그라운드 상시 리스닝 불가
- 따라서 iOS 버전은 "탐지·차단"이 아니라 "사용자 스스로 점검·기록·학습" 도구로 스코프 고정

## 서버(Java) + AI 파인튜닝(Python) 추가분

### server-java/ (Spring Boot)
- `POST /api/v1/events` — 안드로이드 `AnomalyReporter`가 보낼 이상탐지 이벤트 수집 (원본 센서 시계열 포함)
- `POST /api/v1/events/{id}/confirm` — 셀프리포트로 라벨 확정 (파인튜닝용 정답 라벨이 됨)
- `GET /api/v1/events/dataset/export` — Python이 가져갈 라벨링 데이터 JSONL
- `GET /api/v1/model/latest` — 안드로이드가 최신 파인튜닝 모델(.tflite) 다운로드
- 기본 DB는 H2 파일 DB로 스캐폴딩 (실배포 시 PostgreSQL 등으로 교체 권장)
- **아직 없는 것**: 인증/인가(현재 완전 오픈 API), 익명화 처리 로직, Docker/배포 설정

### ml-python/
- `data_loader.py` — 서버 JSONL을 고정 길이 시계열 배열로 변환
- `finetune.py` — 사전학습 HAR 백본 로드 → 분류 헤드만 재학습 → TFLite 변환
- `pretrain_backbone_from_scratch.py` — 공개 사전학습 체크포인트를 못 구했을 때의 대안 뼈대 (미완성, TODO 상태)
- **중요**: "NFC 태깅 의도" 전용 사전학습 모델은 세상에 없음. UCI HAR/HHAR 같은 범용 행동인식
  공개 모델을 백본으로 재활용하는 전이학습 구조이고, 실제 체크포인트 파일은 직접 구해서
  `./pretrained/har_backbone.keras`에 놓아야 함 (자동 다운로드 코드 없음 — 링크 수명·라이선스 문제로 의도적으로 뺌)
- **데이터 자체가 아직 없음**: 지금은 파이프라인 배관만 있는 상태. 실사용자 온보딩+셀프리포트가
  누적돼야 `finetune.py`가 의미 있게 돌아감 (50건 미만이면 스크립트가 경고를 출력하도록 해둠)

## 앱스토어/플레이스토어 제출 전 남은 작업
- Android: Android Studio에서 프로젝트 임포트 → 레이아웃 XML 완성 → 실기기 HCE 테스트
  (에뮬레이터는 NFC 하드웨어 시뮬레이션이 제한적이라 실기기 필수)
- iOS: Mac + Xcode에서 프로젝트 열어서 서명/프로비저닝 프로파일 설정 → 실기기 빌드
  (Core NFC는 시뮬레이터에서 동작 안 함, 실기기 필수)
- 두 플랫폼 다 개인정보처리방침 페이지 필요 (센서/NFC 데이터 수집 고지)
